// extend-expect is not available in jest-dom v6+ by default without proper setup, 
// but since we don't have @testing-library/jest-dom installed yet, we'll keep this empty.
// If needed, we can install @testing-library/jest-dom and import it here.
